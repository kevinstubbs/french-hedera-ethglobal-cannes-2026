import { describe, it } from "vitest";

describe.skip("Phase 4 — live facilitator + chain (not mock-backed CI)", () => {
  it("placeholder: TS agent pays real x402 against staging API", () => {
    // Enable when running manual/staging verification.
  });
});

describe.skip("Phase 4 — real Naryo Configuration API adapter", () => {
  it("placeholder: provision broadcaster to internal ingest URL", () => {
    // See docs/NARYO_VERIFY.md and internal/naryo/client.go.
  });
});
